apiVersion: v1
kind: Service
metadata:
  name: {{ include "octoprint-chart.fullname" . }}
  labels:
    {{- include "octoprint-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "octoprint-chart.selectorLabels" . | nindent 4 }}
